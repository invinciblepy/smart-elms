[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=3.0.0"
flask-sqlalchemy = ">=3.1.1"
flask-bcrypt = ">=1.0.1"
flask-jwt-extended = ">=4.6.0"
sqlalchemy = ">=2.0.0"
werkzeug = ">=3.0.0"
numpy = ">=1.26.0"
pandas = ">=2.1.0"
scikit-learn = ">=1.4.0"
matplotlib = ">=3.8.0"
seaborn = ">=0.13.0"
joblib = ">=1.3.0"
pytest = ">=8.0.0"
pytest-cov = ">=5.0.0"
openpyxl = ">=3.1.0"

[dev-packages]
ipykernel = "*"

[requires]
python_version = "3.13"
